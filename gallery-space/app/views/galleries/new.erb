<div><h1>New Gallery</h1></div>

<div>
  <% if @gallery.errors.any? %>
    <h2><%= pluralize(@gallery.errors.count, "error") %> prohibited this gallery from being saved:</h2>
    <ul>
    <% @gallery.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  <% end %>
</div>

<!--
need to add in @user.id for @gallery.user_id...
add this inside squids to bottom of form:
[SQUID] f.hidden_field(:user_id, :value => @user.id) %>
(need to have the @user set up in Galleries#new method)
...
also, should we make the 'theme' field a drop-down of
pre-set options PLUS the ability to make a new one??
-->



<div>
  <%= form_tag(user_galleries_path(current_user)) do %>
    <%= label_tag(:name)%>
    <%= text_field_tag("gallery[name]") %><br>
    <%= label_tag(:theme)%>
    <%= text_field_tag("gallery[theme]") %><br>
    <%= label_tag(:blurb)%>
    <%= text_field_tag("gallery[blurb]") %><br>
    <%= hidden_field_tag 'gallery[user_id]', current_user.id %>
    <%= submit_tag %>
  <% end %>
</div>

<br>
<div>
  <p><%= link_to 'Back to your Profile', show_your_profile_path(current_user) %></p>
</div>

<br>
<div>
  <p><%= link_to 'Back to Main Page', root_path %></p>
</div>


<!--
<br>
<div>
  <p>[SQUID] link_to 'Gallery Index', user_galleries_path %></p>
</div>
-->
